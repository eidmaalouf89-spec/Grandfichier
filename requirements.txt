# JANSA GrandFichier Updater V1 — Python dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Excel read/write (core dependency)
openpyxl>=3.1.0

# PDF text extraction (for pdf_ingest.py)
pdfplumber>=0.10.0

# API server (for React UI)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Testing
pytest>=7.0.0
